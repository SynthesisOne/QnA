$('.new-answer-errors').html('<%= render 'shared/errors', resource: answer %>')
<% if answer.persisted? %>
$('.answers').append('<%= j render answer %>')
$(' .new-answer #answer_body').val('');
$('.form-control-file').val('');
$('.form-control-file').attr('disabled', false);
<%end %>